import React from 'react';
import AdminDashboard from './AdminDashboard';

// For now, CreatePost is unified into AdminDashboard's form.
const CreatePost = () => <AdminDashboard />;

export default CreatePost;
